using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ShowBump : PhysicsBase
{


    /*


    // classic Ease-In tween
    val = (target - current) / 2


    50 = (100 - 0) / 2
    25 = (100 - 50) / 2
    12.5 = (100 - 25) / 2
    6 = (100 - 12.5) / 2
    3 = (100 - 6) / 2






    */


    //public Vector3 velocityChange;


    //public Vector3 jump;


    //void Start()
    //{

    //}


    //void Update()
    //{
    //    if (Input.GetKeyDown("1"))
    //    {
    //        StartCoroutine(ChangeVectorRange(velocityChange, jump));
    //    }
    //}


    //void FixedUpdate()
    //{
    //    velocityChange += playerInput;

    //    // direction to move each frame = normalized input vector * speed * time since last frame
    //    Vector3 step = playerInput * thrust * Time.deltaTime;

    //    // add step vector to current position
    //    //rb.MovePosition(transform.position + step);

    //    // Rigidbody.velocity is the distance and direction the Rigidbody travels each second,
    //    // including all external forces. Setting this directly will disable gravity.
    //    rb.velocity += playerInput;
    //}


    //IEnumerator ChangeVectorRange(Vector3 velocity, Vector3 modifier)
    //{

    //}


}
